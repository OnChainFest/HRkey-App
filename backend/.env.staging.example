# Launch-0 staging smoke env kit (no secrets)
# Copy to a local .env file and fill placeholders.

BASE_URL="https://staging-api.example.com"
TEST_USER_JWT="REPLACE_WITH_TEST_JWT"
TEST_USER_ID="REPLACE_WITH_TEST_USER_ID"
TEST_OTHER_USER_ID="REPLACE_WITH_OTHER_USER_ID"

# Optional allowlist for rate limit bypass (comma-separated IPs)
# RATE_LIMIT_ALLOWLIST="203.0.113.10,198.51.100.5"

# OpenAI Configuration (for AI refinement feature)
OPENAI_API_KEY="sk-..."
OPENAI_MODEL="gpt-4o-mini"
