cd ~/OneDrive/Escritorio/OnChainFest/HRkey

cat > vercel.json << 'JSON'
{
  "cleanUrls": true,
  "rewrites": [
    { "source": "/",     "destination": "/index.html" },
    { "source": "/auth", "destination": "/auth.html"  },
    { "source": "/(.*)", "destination": "/$1"         }
  ]
}
JSON

git add vercel.json
git commit -m "chore(vercel): rewrites for WebDapp as root (no /WebDapp prefix)"
git push origin main
